COUNT(*), COUNT(column)의 차이

COUNT(*)
: 테이블의 모든 ROW의 수를 센다.

COUNT(column)
: 해당 COLUMN이 NOT NULL인 ROW의 수만 센다.

COUNT(DISTINCT column)
: 해당 COLUMN의 중복을 제거하고 NOT NULL인 ROW의 수를 센다.

COUNT(DISTINCT CASE WHEN condition THEN result)
: 조건에 해당하는 COLUMN의 NOT NULL인 ROW의 수를 센다.
